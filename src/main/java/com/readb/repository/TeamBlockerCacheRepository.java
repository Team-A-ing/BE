package com.readb.repository;

import com.readb.domain.team.TeamBlockerCache;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TeamBlockerCacheRepository extends JpaRepository<TeamBlockerCache, Long> {
}
