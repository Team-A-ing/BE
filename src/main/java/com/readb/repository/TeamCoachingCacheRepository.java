package com.readb.repository;

import com.readb.domain.team.TeamCoachingCache;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TeamCoachingCacheRepository extends JpaRepository<TeamCoachingCache, Long> {
}
